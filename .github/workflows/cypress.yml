name: Cypress
on: [push]
jobs:
  cypress-run-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Cypress run
        uses: cypress-io/github-action@v5.2.0
        with:
          build: npm run build
          start: npm start
